"""The Supplies plugin: paint, cement, tools, and other hobby-supply inventory.

Schema migrations for this plugin's tables live in the repository-wide
``migrations/versions/`` directory rather than inside this package, matching
every other plugin — see
``gunpla_fabrication_suite.core.plugins.discovery.MODEL_MODULES``.
"""

from __future__ import annotations
