"""Workshop Dark: the original, default theme.

Defined in `base.py` itself (re-exported here) so `PALETTE` always has a
real default from the moment the theme package is first imported — see
`base.py`'s comment on `WORKSHOP_DARK` for why.
"""

from __future__ import annotations

from gunpla_fabrication_suite.themes.base import WORKSHOP_DARK

__all__ = ["WORKSHOP_DARK"]
